{
  "name": "@enbox/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared contracts (models, API DTOs, validation schemas, realtime events) for Enbox.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}
