import type { SocketRegistrar } from '../../realtime/types.js';

/** users socket handlers (see ClientToServerEvents in @enbox/shared). */
export const registerUsersSocket: SocketRegistrar = (_io, _socket) => {};
